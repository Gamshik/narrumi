// getSelectedStoryWordsLabel formats the badge from the visible episode selection.
export function getSelectedStoryWordsLabel(selectedWordCount: number): string {
  return `${selectedWordCount} selected`;
}
