---
phase: 04-selection-feasibility-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx
  - apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.styles.ts
  - apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts
  - apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/index.ts
  - apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts
autonomous: true
requirements:
  - SELC-01
  - SELC-02
  - SELC-04
must_haves:
  truths:
    - "Probe A runs first and uses React Native `Text selectable` to establish native handles, highlighting, endpoint behavior, rich nested annotation text, scrolling, and gesture coexistence without claiming observable offsets."
    - "Probe B runs second and uses one multiline read-only `TextInput` per rendered episode to observe `{start,end}` while preserving one stable episode/revision owner."
    - "Per D-03, annotated fragments remain continuous selectable characters and a selection gesture must take precedence over annotation opening while selection is active."
    - "Per D-04, both candidates delegate visible endpoints and handles to the platform; neither paints handles, hit-tests characters, snaps to sentences, or forces word endpoints."
  artifacts:
    - path: "apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx"
      provides: "Ordered React Native core candidates and ephemeral exact-range diagnostics"
      exports: ["EpisodeSelectionProbe"]
    - path: "apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts"
      provides: "Candidate and event contracts"
      exports: ["EpisodeSelectionProbeCandidate", "EpisodeSelectionProbeProps"]
    - path: "apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts"
      provides: "Structural guard against ownership and fallback regressions"
  key_links:
    - from: "apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx"
      to: "apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionRange.ts"
      via: "`TextInput.onSelectionChange` forwards raw offsets with the rendered episode/document identity to strict reconstruction"
      pattern: "onSelectionChange"
    - from: "apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx"
      to: "apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionDocument.ts"
      via: "Both candidates render the same stable canonical document and segment boundaries"
      pattern: "createEpisodeSelectionDocument"
---

<objective>
Implement the two ordered Expo Go selection candidates as one focused screen-local probe component without changing production reader routing.

Purpose: Isolate the React Native core feasibility question: Probe A proves native selection fidelity, then Probe B adds observable offsets and exact reconstruction.
Output: A typed `EpisodeSelectionProbe` component folder plus structural tests that prohibit ownership inference and non-native fallbacks.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md
@.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md
@.planning/phases/04-selection-feasibility-gate/04-PATTERNS.md
@design/design_system_guidelines.md
@apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSentence/EpisodeSentence.tsx
@apps/mobile/src/presentation/app/screens/episodeReader/components/StoryContinuationPrelude/StoryContinuationPrelude.styles.ts
@apps/mobile/src/presentation/app/shared/BubbleSlider/BubbleSlider.types.ts
@apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionDocument.ts
@apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionRange.ts
</context>

<interfaces>
From Plan 02:
- `createEpisodeSelectionDocument(episode)` returns one immutable episode ID/revision/text/segment map.
- `reconstructEpisodeSelection(document, observation)` returns exact raw and separately edge-trimmed text only for a valid matching episode/revision/range.
- `SELECTION_FEASIBILITY_EPISODES` is synthetic, but this component remains parameterized by an `Episode` rather than importing the fixture.

From `EpisodeSentence.tsx`, preserve alias import ordering, `ReactElement` returns, annotated nested story fragments, semantic `AppStyles`, and annotation callbacks. Do not import persistence, services, Supabase, WebView, or native modules.
</interfaces>

## Artifacts this phase produces

- Union type `EpisodeSelectionProbeCandidate` with ordered values `selectable-text` and `observable-text-input`.
- Props type `EpisodeSelectionProbeProps` carrying `episode`, `candidate`, `styles`, and `onPressAnnotation` without header state or persistence.
- Component `EpisodeSelectionProbe` with memoized native text subtree and diagnostics rendered as a sibling.
- Local style type/factory for subordinate feasibility diagnostics using existing semantic tokens and unrestricted font scaling.
- Folder barrel `EpisodeSelectionProbe/index.ts`.
- Structural test file `episodeSelectionProbeSource.test.ts` asserting both React Native core candidates, event ownership, and forbidden-fallback absence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-03-01: Build Probe A as the native selectable-text fidelity baseline</name>
  <read_first>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.styles.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/index.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-PATTERNS.md</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSentence/EpisodeSentence.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/episodeReaderText.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionDocument.ts</file>
    <file>design/design_system_guidelines.md</file>
  </read_first>
  <files>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx, apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.styles.ts, apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts, apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/index.ts, apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts</files>
  <behavior>
    - "`selectable-text` renders one native selectable `Text` container for one episode document, with deterministic internal separators and nested styled narration/dialogue/annotation fragments."
    - "A normal annotation tap still reports the annotation when no selection gesture is active; physical evidence decides whether long-press/handle interaction correctly wins."
    - "The baseline displays that range offsets are not publicly observable and cannot itself satisfy D-01."
  </behavior>
  <action>Write structural tests first, then define the candidate/props contracts and create the component folder. For candidate `selectable-text`, construct one stable selection document with `useMemo`, render one React Native `Text` carrying `selectable`, and emit its exact segments and separators through nested `Text` children so the visible content is the canonical string. Reuse current semantic reader styles for narration, dialogue, and annotated fragments; speaker labels may be rendered outside the selectable container but must not enter its text. Keep annotation `onPress` only on the annotated canonical fragment. Per D-03, do not insert wrapper characters or gaps around annotation fragments. Per D-04, rely exclusively on platform selection handles/highlight and preserve Dynamic Type. Render a sibling diagnostic stating that Probe A has no supported JavaScript range callback; do not infer offsets from clipboard, layout geometry, or string search. Keep the selected text subtree memoized/stable so diagnostic changes cannot rewrite it.</action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npx tsx --test "src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts" &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    <assertion>The structural suite exits 0 and finds a React Native `Text` with native selection enabled for Probe A.</assertion>
    <assertion>Probe A consumes `createEpisodeSelectionDocument` and renders each exact segment plus the exported separator without deriving content from episode title, speaker labels, annotations, or header state.</assertion>
    <assertion>Annotated fragment text is canonical story text and retains its normal annotation callback.</assertion>
    <assertion>No clipboard polling, WebView, custom pan/handle geometry, sentence snapping, storage, network, or service call exists in the component folder.</assertion>
    <assertion>Font scaling remains enabled and no fixed-height story-text container is introduced.</assertion>
  </acceptance_criteria>
  <done>Probe A is an honest native-fidelity baseline that preserves rich story text but explicitly does not claim observable exact offsets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03-02: Add Probe B with episode-owned observable offsets and exact diagnostics</name>
  <read_first>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.styles.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionDocument.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionRange.ts</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-PATTERNS.md</file>
    <file>apps/mobile/node_modules/react-native/Libraries/Components/TextInput/TextInput.js</file>
  </read_first>
  <files>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx, apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.styles.ts, apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts, apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts</files>
  <behavior>
    - "`observable-text-input` renders one multiline read-only, non-internally-scrolling native `TextInput` per episode and reports the platform `{start,end}` event."
    - "Every event is paired with the component's rendered episode ID and document revision before strict reconstruction."
    - "Valid observations show episode ID, revision, raw offsets, JSON-escaped raw substring, and separately escaped edge-trimmed value outside the selected subtree; invalid/stale observations show rejection without clamping."
  </behavior>
  <action>Add the second candidate only after Probe A remains intact. Use one multiline React Native `TextInput` per episode, configured read-only and with internal scrolling disabled, and render the exact same stable canonical document. Forward `TextInputSelectionChangeEvent.nativeEvent.selection` with the closed-over `document.episodeId` and `document.revision` to `reconstructEpisodeSelection`; never consult `activeEpisodeIndex`, active header text, clipboard, or a text search. Keep observation state and diagnostics outside a memoized input surface. Diagnostics must expose JSON-escaped synthetic raw text, edge-trimmed text, raw offsets, owner, and revision without persistence, transport, console logging, or production analytics. Preserve native selection behavior without controlling the `selection` prop. Experiment with rich nested children only as an explicitly labelled pinned-runtime probe; the source and final evidence must not present undocumented child rendering as an official public contract. Physical testing, not this task, determines keyboard suppression, scroll coexistence, annotation precedence, dialogue fidelity, and Dynamic Type stability.</action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npx tsx --test "src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts" "src/presentation/app/screens/episodeReader/selection/episodeSelectionRange.test.ts" &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    <assertion>The structural suite exits 0 and finds one multiline read-only `TextInput`, `scrollEnabled={false}`, and an `onSelectionChange` path through `reconstructEpisodeSelection`.</assertion>
    <assertion>Ownership is closed over from the rendered document; neither the component nor its tests reference active reader header/index state.</assertion>
    <assertion>The component does not set a controlled selection range, normalize raw text, force endpoint granularity, log selected text, or persist/transmit observations.</assertion>
    <assertion>Diagnostics are outside the memoized selected-text subtree and distinguish raw exact text from optional outer-edge trimming.</assertion>
    <assertion>Tests, lint, and typecheck exit 0; native behavior remains explicitly unproven pending Plan 05.</assertion>
  </acceptance_criteria>
  <done>Probe B exposes strict episode-owned native offsets while retaining an explicit empirical status for all physical interaction and presentation criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native `TextInput` selection event → JavaScript probe | Platform offsets and event timing are untrusted and can be stale or malformed. |
| selected synthetic text → on-screen diagnostics | Exact fixture text is displayed locally for audit but must not escape to persistence, logs, analytics, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-TM | Tampering | `EpisodeSelectionProbe` range event | high | mitigate | Bind the rendered document identity, pass raw offsets through the strict Plan 02 utility, and display rejection for invalid/stale events. |
| T-04-03-ID | Information Disclosure | probe diagnostics | medium | mitigate | Run only with synthetic fixture integration, keep diagnostics ephemeral/on-screen, and prohibit logs, analytics, persistence, and transport. |
| T-04-03-EF | Elevation of Functionality | probe component | medium | mitigate | Do not export a production route or activate the probe without the Plan 04 `__DEV__` feasibility seam. |
| T-04-03-DS | Denial of Service | handle-move rerenders | low | accept | Memoize the native text subtree and keep diagnostics outside it; physical evidence detects any remaining selection dismissal. |
</threat_model>

<verification>
- Selection document, range, and structural probe suites pass under `tsx`.
- Lint and strict typecheck pass with explicit annotations and English contract comments.
- Source inspection confirms Probe A precedes Probe B and no native package, WebView, custom handles, persistence, or header-derived ownership was added.
- `cd apps/mobile && npm run lint && npm run typecheck && npm test && npm run build` passes after the wave.
</verification>

<success_criteria>
- Both ordered React Native core candidates exist behind one explicit candidate contract.
- Probe A establishes native-selection fidelity without pretending to expose offsets.
- Probe B can emit an episode/revision-owned exact observation for Plan 05 to validate on hardware.
- All physical behavior remains pending rather than inferred from unit or source tests.
</success_criteria>

<output>
Create `.planning/phases/04-selection-feasibility-gate/04-03-SUMMARY.md` when done.
</output>
