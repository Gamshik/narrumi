---
phase: 04-selection-feasibility-gate
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - apps/mobile/app/episode-reader.tsx
  - apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx
  - apps/mobile/src/presentation/app/screens/episodeReader/components/index.ts
  - apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionReaderIntegration.test.ts
autonomous: true
requirements:
  - SELC-01
  - SELC-02
  - SELC-04
must_haves:
  truths:
    - "The synthetic fixture reaches the real `EpisodeReaderScreen`, `Animated.ScrollView`, episode blocks, active-header behavior, annotations, and existing controls only through an explicit development feasibility route."
    - "Single mode renders exactly one fixture episode; multi mode renders both, and each probe callback owns the episode surface that emitted it rather than the active compact header."
    - "Per D-02, headings, speaker labels, controls, and annotation metadata remain outside the canonical selectable document while narration/dialogue stays one continuous episode-owned range."
    - "Per D-03 and D-04, production reader behavior is unchanged when the feasibility route is absent, and neither candidate replaces the reader with a WebView or custom selection UI."
  artifacts:
    - path: "apps/mobile/app/episode-reader.tsx"
      provides: "Strict `__DEV__` route parsing for fixture, reader mode, and ordered candidate"
    - path: "apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx"
      provides: "Real-reader injection seam and episode-owned probe rendering"
    - path: "apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionReaderIntegration.test.ts"
      provides: "Structural guards for development gating, real reader composition, and header-independent ownership"
  key_links:
    - from: "apps/mobile/app/episode-reader.tsx"
      to: "apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionFixture.ts"
      via: "Exact route tokens plus `__DEV__` select the in-memory synthetic fixture"
      pattern: "__DEV__"
    - from: "apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx"
      to: "apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe"
      via: "Each `episodes.map` block passes its own `episode` and selected candidate"
      pattern: "EpisodeSelectionProbe"
---

<objective>
Mount both selection candidates in the real single- and multi-episode reader using a development-only synthetic-fixture route.

Purpose: Ensure the physical gate exercises the actual scroll/header/episode composition rather than a surrogate screen, while leaving normal reader behavior untouched.
Output: Dev-only route parameters, a focused screen injection seam, component barrel export, and structural integration tests.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md
@.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md
@.planning/phases/04-selection-feasibility-gate/04-PATTERNS.md
@apps/mobile/app/episode-reader.tsx
@apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx
@apps/mobile/src/presentation/app/screens/episodeReader/components/index.ts
@apps/mobile/src/presentation/theme/layout.test.ts
@apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionFixture.ts
@apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/index.ts
</context>

<interfaces>
`EpisodeReaderScreen` currently accepts `episodeOrderIndex`, `seriesId`, `isReadOnly`, `styles`, and `onExit`; it loads episodes from local application services and derives `activeEpisodeIndex` only for header/interaction display. Add one optional feasibility configuration without changing these normal inputs.

`EpisodeSelectionProbe` accepts one rendered episode and one explicit candidate. The integration must call it inside the existing `episodes.map` block and never pass `activeEpisodeIndex` as ownership.
</interfaces>

## Artifacts this phase produces

- Route query fields `selectionProbe`, `selectionProbeCandidate`, and `selectionProbeReaderMode`, each normalized from exact string tokens and ignored outside `__DEV__`.
- Type `SelectionFeasibilityConfig` for fixture ID, candidate, reader mode, and immutable episodes.
- Optional `selectionFeasibility` field on `EpisodeReaderScreenProps`.
- Development URLs documented in code/evidence-facing diagnostics for Probe A/Probe B × single/multi mode.
- Barrel export for `EpisodeSelectionProbe`.
- Structural integration test `episodeSelectionReaderIntegration.test.ts`.

<tasks>

<task type="auto">
  <name>Task 04-04-01: Add the development-only fixture route and real-reader injection seam</name>
  <read_first>
    <file>apps/mobile/app/episode-reader.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/index.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionFixture.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/index.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.types.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/index.ts</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-PATTERNS.md</file>
  </read_first>
  <files>apps/mobile/app/episode-reader.tsx, apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx, apps/mobile/src/presentation/app/screens/episodeReader/components/index.ts</files>
  <action>Re-export `EpisodeSelectionProbe` through the reader component barrel. In the route, extend typed local parameters with exact values: `selectionProbe=true`, candidate `selectable-text` or `observable-text-input`, and reader mode `single` or `multi`. Construct a typed `SelectionFeasibilityConfig` only when `__DEV__` and every token is valid; select the first synthetic episode for single mode and both immutable fixture episodes for multi mode. Pass that optional config to `EpisodeReaderScreen`. In the screen load callback, use the supplied in-memory episodes before local services and set active index 0; otherwise preserve the current loading path byte-for-behavior. Inside each existing episode block, render `EpisodeSelectionProbe` only for feasibility configuration, closing over that mapped episode; retain normal `EpisodeSentence` rendering when absent. Keep the existing `Animated.ScrollView`, headings, `EpisodeReaderEdgeEffects`, `TranslationSheet`, interactions/controls, and active-header calculations. The probe surface may replace only the story-text rendering in the explicit dev experiment, not route metadata or reader chrome. Do not add persistence, network calls, feature flags, production menus, native config, or backend work.</action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    <assertion>Without all valid probe tokens or outside `__DEV__`, the route passes no feasibility configuration and the existing local-service reader path remains active.</assertion>
    <assertion>Single mode passes exactly the first fixture episode; multi mode passes exactly both fixture episodes in stable order.</assertion>
    <assertion>`EpisodeSelectionProbe` is rendered inside each mapped episode block and receives that map item's episode, never `activeEpisodeIndex`, active title, or a global string match as owner.</assertion>
    <assertion>The `Animated.ScrollView`, episode headings, edge effects, annotation sheet, and existing interaction/control rendering remain present.</assertion>
    <assertion>No WebView, native module, `ios/`/`android/` project, EAS config, storage write, network request, or Supabase call is added.</assertion>
  </acceptance_criteria>
  <done>Expo Go can open each candidate in the actual single or multi reader against synthetic episodes while normal routes remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-04-02: Protect real-reader integration and header-independent ownership structurally</name>
  <read_first>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionReaderIntegration.test.ts</file>
    <file>apps/mobile/app/episode-reader.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx</file>
    <file>apps/mobile/src/presentation/theme/layout.test.ts</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-VALIDATION.md</file>
  </read_first>
  <files>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionReaderIntegration.test.ts</files>
  <behavior>
    - "Source checks prove the route requires `__DEV__` plus exact candidate/mode tokens before fixture injection."
    - "Source checks prove the screen retains `Animated.ScrollView`, episode headings, edge effects, annotation sheet, interactions, and maps the current `episode` into `EpisodeSelectionProbe`."
    - "Source checks reject use of active-header/index state inside the probe ownership callback and reject WebView/native fallback imports."
  </behavior>
  <action>Create a Node structural test following `layout.test.ts`: read the route and reader sources as UTF-8 and assert the explicit development gate, exact candidate/mode tokens, single versus two-episode fixture selection, component barrel import, and per-episode probe rendering. Assert the pre-existing scroll, headings, `EpisodeReaderEdgeEffects`, `TranslationSheet`, and interaction blocks remain. Scope ownership assertions to the feasibility configuration/render region so legitimate header code elsewhere in `EpisodeReaderScreen.tsx` does not create a false failure. Add structural absence checks for WebView/native-adapter imports and committed native-project routing, but do not use a file-wide negative assertion for `activeEpisodeIndex` because the header legitimately needs it.</action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npx tsx --test "src/presentation/app/screens/episodeReader/selection/episodeSelectionReaderIntegration.test.ts" "src/presentation/app/screens/episodeReader/selection/episodeSelectionProbeSource.test.ts" &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    <assertion>The focused structural suites report zero failures and exit 0.</assertion>
    <assertion>The test fails if the route loses `__DEV__`, if multi mode no longer supplies both episodes, or if ownership is switched to header/index state.</assertion>
    <assertion>The test fails if the integration removes the existing scroll, headings, edge effects, annotation sheet, or interaction rendering.</assertion>
    <assertion>Ownership checks are region-scoped and do not ban legitimate active-header code elsewhere in the reader.</assertion>
    <assertion>Typecheck exits 0.</assertion>
  </acceptance_criteria>
  <done>Automated checks protect the real-reader seam and ownership wiring while explicitly leaving native gesture success to physical evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route query → development fixture configuration | Arbitrary route strings must not enable hidden production behavior or malformed probe modes. |
| fixture configuration → existing reader | Synthetic episodes bypass local storage only in the explicit development experiment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-EF | Elevation of Functionality | `episode-reader.tsx` probe route | high | mitigate | Require compile-time `__DEV__` and exact allowlisted tokens; no production menu or default activation. |
| T-04-04-TM | Tampering | candidate/mode query values | medium | mitigate | Normalize exact string unions and omit the entire configuration for invalid values. |
| T-04-04-ID | Information Disclosure | `EpisodeReaderScreen` fixture seam | medium | mitigate | Inject only committed synthetic episodes and prohibit local-store reads/writes, logs, network, analytics, and user content in probe mode. |
| T-04-04-DS | Denial of Service | real reader scroll/selection integration | low | accept | Preserve existing scroll composition and require physical-device gesture/viewport validation in Plan 05. |
</threat_model>

<verification>
- Focused integration and probe structural suites pass.
- Full `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` pass after the wave.
- Source review confirms zero schema paths, package installs, native projects, WebView, backend, persistence, or cross-episode selection work.
</verification>

<success_criteria>
- Physical testers can open four explicit Expo Go scenarios: Probe A single, Probe A multi, Probe B single, and Probe B multi.
- All scenarios run inside the existing reader scroll/header/episode surface with synthetic content.
- Multi-episode ownership originates from each rendered episode surface and cannot follow the active header.
- Normal reader behavior remains unchanged when the development probe query is absent.
</success_criteria>

<output>
Create `.planning/phases/04-selection-feasibility-gate/04-04-SUMMARY.md` when done.
</output>
