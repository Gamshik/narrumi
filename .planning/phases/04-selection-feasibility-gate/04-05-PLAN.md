---
phase: 04-selection-feasibility-gate
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - .planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md
autonomous: false
requirements:
  - SELC-01
  - SELC-02
  - SELC-04
must_haves:
  truths:
    - "On representative physical iOS and Android devices, Probe A is exercised before Probe B in both single- and multi-episode readers; automated tests are never accepted as a substitute."
    - "Per D-01, every accepted Probe B row pairs the visible native highlight with matching episode/revision offsets and the exact raw canonical substring, including punctuation and internal separators; only separately reported outer-edge invisible whitespace may be trimmed."
    - "Per D-02, required ranges cross sentence and narration/dialogue layout boundaries inside one episode while headings, speaker labels, controls, and annotation metadata remain excluded."
    - "Per D-03, ranges can start, end, and pass through annotated fragments; annotation tap works without selection and yields to selection gestures only while selection is active."
    - "Per D-04, users adjust platform-native endpoints and handles without custom precision, sentence snapping, forced word endpoints, keyboard/viewport disruption, or reader interaction loss."
    - "A GO is recorded only when both platforms pass every required row in both reader modes; otherwise the single verdict is NO-GO and Phase 5 remains blocked pending explicit approval for any EAS/native escalation."
  artifacts:
    - path: ".planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md"
      provides: "Completed auditable iOS/Android Probe A and Probe B matrices plus one final verdict"
      contains: "## Final Verdict"
  key_links:
    - from: ".planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md"
      to: "apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionRange.ts"
      via: "Each exactness row records the same episode/revision/start/end/raw slice contract displayed by Probe B"
      pattern: "start.*end.*raw"
    - from: ".planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md"
      to: ".planning/ROADMAP.md"
      via: "Only an explicit all-platform GO releases Phase 5"
      pattern: "Phase 5.*blocked"
---

<objective>
Run the complete physical-device matrix in strict probe order and issue one auditable GO or NO-GO for the Expo Managed core approach.

Purpose: Decide whether Phase 5 may begin without silently weakening exact-range, ownership, native interaction, or reader-regression criteria.
Output: Completed synthetic evidence with linked media and a single final verdict; GO unlocks downstream planning, while NO-GO stops for explicit escalation approval.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md
@.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md
@.planning/phases/04-selection-feasibility-gate/04-VALIDATION.md
@.planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md
@apps/mobile/README.md
@apps/mobile/package.json
</context>

<interfaces>
Evidence consumes only the on-screen Probe B diagnostic fields produced in Plan 03: fixture ID, candidate, episode ID, document revision, start, end, JSON-escaped raw text, JSON-escaped edge-trimmed text, and rejection status. The visible native highlight and handles must be shown in paired media; neither diagnostics alone nor screenshots alone prove exactness.
</interfaces>

## Artifacts this phase produces

- Completed `04-FEASIBILITY-EVIDENCE.md` with device model, OS, Expo Go, app commit, operator, and fixture ID.
- Probe A matrix rows for native handles/highlight, scrolling, annotations, dialogue/narration presentation, controls, and Dynamic Type on physical iOS and Android in single then multi mode.
- Probe B matrix rows with `readerMode`, `episodeId`, `documentRevision`, `start`, `end`, escaped raw substring, escaped edge-trimmed substring, visible-match result, Dynamic Type setting, interaction result, and linked screenshot/recording.
- Required exactness scenarios for punctuation, sentence boundary, annotation start/end/pass-through, narration↔dialogue boundary, viewport-edge handle drag, annotation gesture precedence, earlier-episode ownership after header focus changes, default/enlarged Dynamic Type, and nearby reader controls/narration baseline.
- One literal final verdict, `GO` or `NO-GO`, with rationale, operator confirmation, app commit, and downstream Phase 5 status.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 04-05-01: Execute the ordered physical iOS and Android probe matrix</name>
  <read_first>
    <file>.planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-VALIDATION.md</file>
    <file>apps/mobile/app/episode-reader.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/EpisodeReaderScreen.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/components/EpisodeSelectionProbe/EpisodeSelectionProbe.tsx</file>
    <file>apps/mobile/src/presentation/app/screens/episodeReader/selection/episodeSelectionFixture.ts</file>
  </read_first>
  <files>.planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md</files>
  <action>Start the documented Expo development server and run the four development scenarios on the same app commit and fixture: Probe A single, Probe A multi, Probe B single, then Probe B multi. Repeat that exact order on the confirmed physical iOS device and physical Android device. For Probe A, record native handle/highlight fidelity and the absence of a supported range event; do not promote it as an exact-range solution. For Probe B, pair every on-screen diagnostic with media showing the native highlight/handles and populate all evidence fields. On each platform cover punctuation; sentence boundary; start/end/pass-through of annotated text; narration-to-dialogue and dialogue-to-narration without speaker labels; scroll before selection, handle drag near viewport edges, dismissal, and scroll after; annotation tap with no active selection and selection gesture on the same fragment; single mode; earlier and later episodes in multi mode while the compact header is focused elsewhere; default and enlarged Dynamic Type; every usable reader control; and the narration/audio behavior established in Plan 01. Preserve native endpoint choices per D-04 rather than demanding custom character placement. Use only the synthetic fixture and never record user episodes. Mark each row PASS or FAIL; do not omit a failing row, substitute simulator/emulator evidence, or treat automated results as device proof.</action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npx tsx --test "src/presentation/app/screens/episodeReader/selection/**/*.test.ts" &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
    <manual>On both named physical devices, verify every Probe A row before Probe B and confirm each Probe B exactness row has both diagnostic data and linked visible-highlight media.</manual>
  </verify>
  <acceptance_criteria>
    <assertion>The selection quick suite and full lint/typecheck/test/build chain exit 0 on the exact evidence commit.</assertion>
    <assertion>Both physical platform sections contain completed single- and multi-reader rows; no simulator or emulator row is counted as evidence.</assertion>
    <assertion>Every Probe B PASS row has fixture ID, candidate, reader mode, episode ID, revision, integer start/end, JSON-escaped raw text, separately escaped edge-trimmed text, visible exact-match result, Dynamic Type setting, interaction results, and a media link.</assertion>
    <assertion>The earlier-episode multi row shows an owner different from the compact active header and still reconstructs the exact highlighted episode substring.</assertion>
    <assertion>Annotation start/end/pass-through and gesture-precedence cases are present on both platforms.</assertion>
    <assertion>Any keyboard appearance, viewport resize, internal input scrolling, selection dismissal, text mutation, missing rich presentation, control breakage, or offset/highlight mismatch is recorded as FAIL rather than normalized away.</assertion>
    <assertion>No evidence row contains real user episode text, and no selected text was persisted, logged, analyzed, or transmitted.</assertion>
  </acceptance_criteria>
  <done>The evidence file contains a complete, paired, synthetic physical-device matrix for both ordered probes on both platforms; no verdict is inferred until Task 04-05-02.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 04-05-02: Record the strict GO or NO-GO and enforce the downstream stop</name>
  <read_first>
    <file>.planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-CONTEXT.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-RESEARCH.md</file>
    <file>.planning/phases/04-selection-feasibility-gate/04-VALIDATION.md</file>
  </read_first>
  <files>.planning/phases/04-selection-feasibility-gate/04-FEASIBILITY-EVIDENCE.md</files>
  <action>Present the completed matrix without averaging or waiving failures. Record `GO` only if Probe B yields stable observable exact non-empty ranges and every D-01 through D-04 plus SELC-01, SELC-02, and SELC-04 row passes on both physical platforms in both reader modes, with no narration/control baseline blocker. If the only passing presentation depends on undocumented rich `TextInput` children, surface that dependency explicitly and require the user to decide whether the pinned-runtime empirical behavior is acceptable; record their rationale with the verdict. Record `NO-GO` for any unresolved FAIL, missing platform/mode, unstable selection, exactness mismatch, header-derived ownership, sentence-only reduction, WebView/native/custom-handle need, reader regression, or unapproved supportability risk. On NO-GO, state that Phase 4 requirements are not achieved and Phase 5 remains blocked, then stop and request separate explicit approval before installing/configuring EAS development builds or planning a narrow native adapter. Do not implement escalation in this plan. On GO, state which exact candidate and pinned runtime passed and that Phase 5 may be planned; retain the evidence and synthetic fixture for audit.</action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
    <manual>Confirm the operator/user selected exactly one verdict after reviewing all matrix failures, supportability notes, and the Phase 5 consequence.</manual>
  </verify>
  <acceptance_criteria>
    <assertion>`Final Verdict` contains exactly `GO` or `NO-GO`, the app commit, date, operator/user confirmation, passing candidate or failure summary, and rationale.</assertion>
    <assertion>`GO` appears only when every required iOS and Android single/multi row is PASS and exact highlighted raw text equals the canonical slice.</assertion>
    <assertion>`NO-GO` explicitly says Phase 4 requirements remain unachieved and Phase 5 is blocked.</assertion>
    <assertion>A NO-GO contains no EAS installation, development-build configuration, native adapter implementation, backend work, WebView substitution, or sentence-only fallback.</assertion>
    <assertion>If undocumented rich-child behavior is decisive, the evidence records the user's explicit supportability decision and rationale rather than an automatic assumption.</assertion>
    <assertion>The final full mobile verification chain exits 0; this automated result supplements but does not determine the verdict.</assertion>
  </acceptance_criteria>
  <done>A single auditable verdict controls downstream work: GO releases Phase 5 planning; NO-GO stops with explicit approval required for any native escalation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical OS selection UI → evidence matrix | Visible platform behavior and JavaScript diagnostics must be paired without normalization. |
| operator/device → repository evidence | Human observations, device metadata, and media links enter the gate record. |
| gate verdict → Phase 5/native escalation | A mistaken GO could release downstream work; a NO-GO must not silently authorize broader technology. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-TM | Tampering | exactness evidence rows | high | mitigate | Pair visible-highlight media with raw episode/revision/offset diagnostics and exact slice; prohibit normalization and omitted failures. |
| T-04-05-SP | Spoofing | physical-platform evidence | high | mitigate | Require named physical device model/OS/Expo Go/operator and reject simulator/emulator substitutions. |
| T-04-05-ID | Information Disclosure | recordings and escaped ranges | high | mitigate | Use only authored synthetic fixture content and prohibit user episodes, production logs, persistence, analytics, and transmission. |
| T-04-05-EF | Elevation of Functionality | GO/NO-GO boundary | high | mitigate | GO requires unanimous required-row PASS; NO-GO keeps Phase 5 blocked and requires separate explicit approval before EAS/native work. |
| T-04-05-RP | Repudiation | final verdict | medium | mitigate | Record app commit, date, operator/user confirmation, rationale, and linked evidence in one committed artifact. |
</threat_model>

<verification>
- All selection unit/structural tests and the full documented mobile lint/typecheck/test/build chain pass on the evidence commit.
- Both named physical platforms complete Probe A before Probe B in single then multi mode.
- Exact-range diagnostics and visible native highlights match for every GO row.
- Evidence covers scrolling, handles, annotations, dialogue/narration, controls, active-header divergence, and Dynamic Type.
- The verdict is exactly GO or NO-GO and enforces the roadmap dependency.
</verification>

<success_criteria>
- GO: Learners demonstrably select and adjust exact non-empty continuous passages on both physical platforms in both reader modes without core interaction or presentation regression, satisfying SELC-01, SELC-02, SELC-04, and D-01 through D-04.
- NO-GO: The feasibility experiment is complete and auditable, Phase 4 requirements are not claimed, Phase 5 remains blocked, and no native/backend escalation occurs without new explicit approval.
- In either outcome, evidence is synthetic/minimal and contains no user episode passage or persisted/transmitted selection.
</success_criteria>

<output>
Create `.planning/phases/04-selection-feasibility-gate/04-05-SUMMARY.md` when done. If the verdict is NO-GO, the summary must state `blocked` and must not mark Phase 4 complete.
</output>
