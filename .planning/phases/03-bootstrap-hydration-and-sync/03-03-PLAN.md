---
wave: 3
depends_on: ["03-02-PLAN.md"]
files_modified:
  - apps/mobile/src/presentation/app/bootstrap/SettingsSkeleton/SettingsSkeleton.tsx
  - apps/mobile/src/presentation/app/bootstrap/SettingsSkeleton/index.ts
autonomous: true
---

# 03-03-PLAN: Bubble/Sorbet block-level skeletons for hydration states

## Goal
Implement block-level skeleton components (e.g., `SettingsSkeleton`) matching the Bubble/Sorbet visual primitives. This ensures the interface does not "jump" after loading, fulfilling Decision D-01.

## Requirements
- BOOT-01
- BOOT-03

## must_haves
- We MUST build detailed block-level skeletons instead of a generic full-screen loader.
- The `SettingsSkeleton` MUST approximate the layout of the loaded Settings screen (toggles, list items) to prevent visual jumping.

## Artifacts this phase produces
- Component `SettingsSkeleton`

## Tasks

<task>
  <id>1</id>
  <title>Implement SettingsSkeleton</title>
  <read_first>
    <file>apps/mobile/src/presentation/app/shared/BubbleSurface/BubbleSurface.tsx</file>
  </read_first>
  <action>
    Create `apps/mobile/src/presentation/app/bootstrap/SettingsSkeleton/SettingsSkeleton.tsx` and its `index.ts`.
    It should render a static skeleton layout that mimics the Settings screen (e.g., placeholder rows, toggle shapes) using the app's theme and styles.
    It should use `BubbleSurface` or similar existing container components to match the structural layout of the actual settings lists.
  </action>
  <acceptance_criteria>
    <assertion>Typecheck passes for `apps/mobile/src/presentation/app/bootstrap/SettingsSkeleton/SettingsSkeleton.tsx`</assertion>
    <assertion>`SettingsSkeleton` is exported from `index.ts`</assertion>
    <assertion>Every explicit TypeScript annotation is accompanied by an English comment.</assertion>
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <title>Manual UI Verification of Skeletons</title>
  <action>
    Document the manual verification steps required to test `SettingsSkeleton` rendering in the Expo app. Run the Expo app and manually verify that the skeleton matches the layout of the loaded Settings screen without visual jumping.
  </action>
  <acceptance_criteria>
    <assertion>Developer has manually verified the `SettingsSkeleton` visual appearance in the Expo app and confirmed it prevents UI jumping.</assertion>
  </acceptance_criteria>
</task>
