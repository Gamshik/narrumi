---
phase: 03-bootstrap-hydration-and-sync
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/application/useCases/hydrateBootstrapSession.ts
  - apps/mobile/src/application/useCases/hydrateBootstrapSession.test.ts
  - apps/mobile/src/application/useCases/index.ts
  - apps/mobile/src/infrastructure/series/asyncStorageLocalSeriesStore.ts
  - apps/mobile/src/infrastructure/sync/queuedLocalSeriesStore.ts
  - apps/mobile/src/presentation/app/services/localAppServices.ts
autonomous: true
requirements:
  - BOOT-02
  - SET-01
  - SET-02
  - SYNC-02
requirements_addressed:
  - BOOT-02
  - SET-01
  - SET-02
  - SYNC-02
risks:
  - "Preference corruption is currently collapsed into first-run absence; this plan must preserve a recovery signal for D-10 without leaking AsyncStorage details into presentation."
commands:
  - cd apps/mobile && npm run lint
  - cd apps/mobile && npm run test
  - cd apps/mobile && npm run typecheck
  - cd apps/mobile && npm run build
must_haves:
  truths:
    - "BOOT-02, SET-01, D-05, D-09: local preferences hydrate or initialize before any remote sync wait can affect guarded settings-visible surfaces."
    - "SET-02, D-10: invalid stored local preferences reset to fresh product defaults and return a recovered status that later UI can show as a non-blocking warning."
    - "SYNC-02, D-08: local bootstrap hydration does not require online connectivity or authenticated remote sync before completing."
  artifacts:
    - path: apps/mobile/src/application/useCases/hydrateBootstrapSession.ts
      provides: "Typed application use case for local-only bootstrap preference hydration and recovery metadata."
    - path: apps/mobile/src/application/useCases/hydrateBootstrapSession.test.ts
      provides: "Regression tests for first-run default creation, existing preference loading, corruption recovery, and no pre-sync dependency."
    - path: apps/mobile/src/presentation/app/services/localAppServices.ts
      provides: "Composition-root export for bootstrap-safe local hydration that bypasses withPreSync."
  key_links:
    - from: apps/mobile/src/presentation/app/services/localAppServices.ts
      to: apps/mobile/src/application/useCases/hydrateBootstrapSession.ts
      via: "localAppServices.hydrateBootstrapSession.execute calls the local-only bootstrap hydration use case directly"
      pattern: "hydrateBootstrapSession"
---

<objective>
Create the local-only bootstrap hydration contract for Phase 03.

Purpose: This plan satisfies BOOT-02, SET-01, SET-02, SYNC-02, D-05, D-08, D-09, and D-10 by giving the authenticated root a use case that reads or creates local preferences without the current `withPreSync` remote wait and without treating placeholder defaults as loaded user data.
Output: A tested `hydrateBootstrapSession` application use case, preference corruption recovery metadata, and a composition-root service that later plans can call before guarded settings-visible surfaces render.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-bootstrap-hydration-and-sync/03-CONTEXT.md
@.planning/phases/03-bootstrap-hydration-and-sync/03-RESEARCH.md
@concept/prd_concept_mvp.md
@stack/tech_stack_mvp.md
@architecture/architecture_for_ai.md
@apps/mobile/README.md
@apps/mobile/package.json
@apps/mobile/src/application/useCases/loadLearningPreferences.ts
@apps/mobile/src/infrastructure/series/asyncStorageLocalSeriesStore.ts
@apps/mobile/src/infrastructure/sync/queuedLocalSeriesStore.ts
@apps/mobile/src/presentation/app/services/localAppServices.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bootstrap hydration tests before implementation</name>
  <files>apps/mobile/src/application/useCases/hydrateBootstrapSession.test.ts</files>
  <behavior>
    - Existing valid local preferences return a loaded result without creating new defaults.
    - Missing preferences create product defaults as dirty local data and identify the result as first-run created data per D-09.
    - Invalid local preferences create fresh defaults and identify the result as recovered data per D-10.
    - The use case never calls sync or remote-store doubles before returning local preferences per D-05 and D-08.
  </behavior>
  <action>Create a Node test runner test file beside the new use case using `node:test` and `node:assert/strict`. Build typed in-memory store doubles and a deterministic `Clock`. Keep comments in English for every explicit TypeScript annotation. The tests should initially fail because `createHydrateBootstrapSession` and its result contract do not exist yet.</action>
  <verify>
    <automated>cd apps/mobile && npm run test -- src/application/useCases/hydrateBootstrapSession.test.ts</automated>
  </verify>
  <done>Tests fail for missing bootstrap hydration use case before production implementation, and each case maps to BOOT-02, SET-01, SET-02, SYNC-02, D-05, D-08, D-09, or D-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement local-only bootstrap hydration use case</name>
  <files>apps/mobile/src/application/useCases/hydrateBootstrapSession.ts, apps/mobile/src/application/useCases/index.ts, apps/mobile/src/infrastructure/series/asyncStorageLocalSeriesStore.ts, apps/mobile/src/infrastructure/sync/queuedLocalSeriesStore.ts</files>
  <behavior>
    - `createHydrateBootstrapSession` returns a discriminated result such as loaded, created, or recovered with `LearningPreferences`.
    - Corrupt preference storage is recovered to defaults and marks recovery without exposing raw parse or storage errors to presentation.
    - The created or recovered defaults are saved locally through the queued local store so sync metadata remains dirty for later background sync.
  </behavior>
  <action>Add a focused `createHydrateBootstrapSession` use case in the application layer. Define a narrow use-case-local preference hydration store contract that supports reading preferences with recovery metadata and saving preferences; do not import infrastructure into application. Extend `AsyncStorageLocalSeriesStore` with a bootstrap read method that validates the existing preference record, removes invalid storage, and returns a safe recovery flag. Extend `QueuedLocalSeriesStore` to forward that read method when its wrapped store supports it and to keep default saves queued. Export the use case from the use-case barrel. Preserve `loadLearningPreferences` for existing callers, but do not use `withPreSync` for bootstrap hydration.</action>
  <verify>
    <automated>cd apps/mobile && npm run test -- src/application/useCases/hydrateBootstrapSession.test.ts</automated>
  </verify>
  <done>The new use case passes all local hydration tests, returns local preferences before any remote work, and exposes created/recovered metadata needed by later UI.</done>
</task>

<task type="auto">
  <name>Task 3: Wire bootstrap hydration service without pre-sync</name>
  <files>apps/mobile/src/presentation/app/services/localAppServices.ts</files>
  <action>Instantiate `hydrateBootstrapSession` in `localAppServices.ts` using the queued local store and system clock, then export it as `localAppServices.hydrateBootstrapSession` without `withPreSync` and without `withBackgroundSync`. Keep the existing `loadLearningPreferences` export unchanged for non-bootstrap reads. This implements D-05 and D-08 by making local bootstrap release independent from remote sync.</action>
  <verify>
    <automated>cd apps/mobile && npm run test -- src/application/useCases/hydrateBootstrapSession.test.ts</automated>
  </verify>
  <done>`localAppServices.hydrateBootstrapSession.execute()` exists, is local-only, and does not call `syncLocalChanges` before returning preferences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage -> application | Local preference JSON is mutable user-device data and must be validated before use. |
| application -> presentation | Recovery and error summaries must be safe UI-facing state, not raw storage/parser errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `AsyncStorageLocalSeriesStore` preference read | medium | mitigate | Validate stored preferences before returning them; remove invalid storage and create fresh local defaults with recovered metadata per D-10. |
| T-03-02 | Information Disclosure | `hydrateBootstrapSession` errors | low | mitigate | Return safe recovery state or throw generic local hydration errors; do not surface raw parser or AsyncStorage details. |
| T-03-SC | Tampering | npm installs | high | mitigate | No package-manager installs are allowed in this plan; run existing package scripts only. |
</threat_model>

<verification>
Run after this plan:
<automated>cd apps/mobile && npm run test -- src/application/useCases/hydrateBootstrapSession.test.ts</automated>
<automated>cd apps/mobile && npm run lint</automated>
<automated>cd apps/mobile && npm run typecheck</automated>
</verification>

<success_criteria>
BOOT-02, SET-01, SET-02, SYNC-02, D-05, D-08, D-09, and D-10 are satisfied at the application contract level: local preferences hydrate or initialize before sync, invalid local data recovers with metadata, and no bootstrap caller needs placeholder UI defaults.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Restore local user state before settings-visible screens render and reconcile remote data without default flicker | 03-01, 03-02, 03-03, 03-04 | COVERED | This plan covers the local hydration foundation.
REQ | BOOT-02 | Local settings/session data restored before server-backed values | 03-01 | COVERED | Local-only hydration contract.
REQ | SET-01 | No placeholder defaults during hydration | 03-01, 03-04 | COVERED | Removes need for screen-local placeholders.
REQ | SET-02 | Explicit loading/loaded/error/offline-aware states | 03-01, 03-03, 03-04 | COVERED | Provides recovered metadata.
REQ | SYNC-02 | Offline entry does not hang on remote sync | 03-01, 03-02 | COVERED | No pre-sync dependency.
RESEARCH | local-only hydration service | 03-01 | COVERED | Implements recommended split from `withPreSync`.
RESEARCH | corruption recovery metadata | 03-01, 03-04 | COVERED | D-10 recovery signal.
CONTEXT | D-05 | Release after local hydration only | 03-01, 03-02 | COVERED | Bootstrap service bypasses pre-sync.
CONTEXT | D-08 | No guarded surface requires remote sync before opening | 03-01, 03-02, 03-04 | COVERED | Local preferences sufficient.
CONTEXT | D-09 | Missing local prefs become local defaults and loaded state | 03-01, 03-04 | COVERED | Use case creates defaults.
CONTEXT | D-10 | Invalid preferences recover to defaults with warning | 03-01, 03-04 | COVERED | Recovery flag.
</source_audit>

<output>
Create `.planning/phases/03-bootstrap-hydration-and-sync/03-01-SUMMARY.md` when done.
</output>
