---
phase: 01-bubble-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - apps/mobile/src/presentation/theme/tokens.ts
  - apps/mobile/src/presentation/theme/tokens.test.ts
  - apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx
  - apps/mobile/src/presentation/app/shared/BubbleToggle/BubbleToggle.tsx
  - apps/mobile/src/presentation/app/shared/BubbleSheet/BubbleSheet.tsx
  - apps/mobile/src/presentation/app/shared/DictionaryWordDetailsSheet.tsx
  - apps/mobile/app/dictionary-word-details.tsx
autonomous: true
requirements:
  - VIS-02
  - VIS-03
  - MOT-01
  - MOT-02
  - QUAL-01
must_haves:
  truths:
    - "MOT-01: Bubble/Sorbet press feedback is noticeably tactile across JellyPressable-backed controls, not merely technically animated."
    - "VIS-03: Settings Dark Mode toggle animates its on/off transition so the state change does not feel laggy."
    - "MOT-02: Dictionary word detail sheets inside the native formSheet route measure to intrinsic content instead of a full-screen wrapper."
    - "QUAL-01: Gap closure stays in presentation/app route code and does not add dependencies, native project files, persistence, sync, AI, Supabase, or domain behavior."
  artifacts:
    - path: "apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx"
      provides: "Stronger token-driven tactile press/release behavior."
    - path: "apps/mobile/src/presentation/app/shared/BubbleToggle/BubbleToggle.tsx"
      provides: "Animated controlled custom switch transition."
    - path: "apps/mobile/src/presentation/app/shared/BubbleSheet/BubbleSheet.tsx"
      provides: "Content-sized rendering mode for native formSheet consumers."
    - path: "apps/mobile/app/dictionary-word-details.tsx"
      provides: "Explicit dictionary loading state separate from not-found state."
  key_links:
    - from: "apps/mobile/src/presentation/theme/tokens.ts"
      to: "apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx"
      via: "shared motion tokens"
      pattern: "motion.pressScale"
    - from: "apps/mobile/src/presentation/app/shared/BubbleToggle/BubbleToggle.tsx"
      to: "apps/mobile/src/presentation/app/screens/SettingsScreen.tsx"
      via: "existing controlled value/onValueChange contract"
      pattern: "BubbleToggle"
    - from: "apps/mobile/src/presentation/app/shared/BubbleSheet/BubbleSheet.tsx"
      to: "apps/mobile/app/_layout.tsx"
      via: "native formSheet fitToContents measurement"
      pattern: "sheetAllowedDetents: 'fitToContents'"
---

<objective>
Close the diagnosed Phase 01 UAT gaps left after Plan 04.

Purpose: Make the Bubble/Sorbet controls and dictionary sheet pass human verification by fixing the remaining perceived-tactility, toggle-transition, and native formSheet measurement problems without changing product scope.
Output: A focused executable gap-closure plan for `$gsd-execute-phase 01 --gaps-only`.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-bubble-foundation/01-UAT.md
@.planning/debug/phase-01-tactile-feedback.md
@.planning/debug/phase-01-toggle-animation.md
@.planning/debug/phase-01-dictionary-sheet.md
@.planning/phases/01-bubble-foundation/01-04-SUMMARY.md
@AGENTS.md
@design/design_system.html
@design/design_system_guidelines.md
@stack/tech_stack_mvp.md
@architecture/architecture_for_ai.md
@apps/mobile/package.json
@apps/mobile/src/presentation/theme/tokens.ts
@apps/mobile/src/presentation/theme/tokens.test.ts
@apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx
@apps/mobile/src/presentation/app/shared/BubbleToggle/BubbleToggle.tsx
@apps/mobile/src/presentation/app/shared/BubbleSheet/BubbleSheet.tsx
@apps/mobile/src/presentation/app/shared/DictionaryWordDetailsSheet.tsx
@apps/mobile/app/dictionary-word-details.tsx
@apps/mobile/app/_layout.tsx
</context>

## Diagnosed Gaps

| Test | Severity | Root Cause | Primary Files |
| --- | --- | --- | --- |
| 1. Tactile Bubble/Sorbet press response | minor | Shared press feedback is still mostly one scale-down effect with conservative values and partly hardcoded release behavior. | `tokens.ts`, `JellyPressable.tsx` |
| 2. Settings custom BubbleToggle in both themes | minor | Toggle thumb and track state jump directly from `value` with no animated transition. | `BubbleToggle.tsx` |
| 3. iOS dictionary details form sheet sizing | major | `BubbleSheet` still renders a full-screen absolute root inside the native `fitToContents` formSheet route, and loading/not-found state is conflated. | `BubbleSheet.tsx`, `dictionary-word-details.tsx` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make JellyPressable feedback visibly tactile</name>
  <files>apps/mobile/src/presentation/theme/tokens.ts, apps/mobile/src/presentation/theme/tokens.test.ts, apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx</files>
  <read_first>
    - `apps/mobile/src/presentation/theme/tokens.ts`
    - `apps/mobile/src/presentation/theme/tokens.test.ts`
    - `apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx`
    - `.planning/debug/phase-01-tactile-feedback.md`
  </read_first>
  <behavior>
    - Pressing Bubble/Sorbet controls creates a visible, responsive down-and-release effect.
    - The animation remains transform-based and layout-stable.
    - Shared motion tokens remain the source of tactile timing and scale decisions.
  </behavior>
  <action>Strengthen the shared tactile motion contract just enough to satisfy the UAT feedback. Update the token test so it protects a clearly visible press scale and release contract. Route `JellyPressable` press-in and press-out spring settings through the shared `motion` tokens instead of leaving the release values hardcoded. If adding another tactile cue, keep it native-driver compatible and avoid layout properties. Do not add dependencies or screen-local animation constants.</action>
  <verify>
    <automated>cd apps/mobile; npm run test -- src/presentation/theme/tokens.test.ts</automated>
    <automated>cd apps/mobile; npm run lint</automated>
    <automated>cd apps/mobile; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Token tests fail if the tactile feedback drifts back to subtle Plan 04 levels.
    - `JellyPressable` remains typed, accessible through forwarded Pressable props, and presentation-only.
    - Press/release behavior is centralized in shared tokens or explicit shared primitive logic.
  </acceptance_criteria>
  <done>UAT Test 1 has a concrete code fix path for stronger tactile feedback.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Animate BubbleToggle state transitions</name>
  <files>apps/mobile/src/presentation/app/shared/BubbleToggle/BubbleToggle.tsx</files>
  <read_first>
    - `apps/mobile/src/presentation/app/shared/BubbleToggle/BubbleToggle.tsx`
    - `apps/mobile/src/presentation/app/shared/JellyPressable/JellyPressable.tsx`
    - `.planning/debug/phase-01-toggle-animation.md`
  </read_first>
  <behavior>
    - Tapping the Settings Dark Mode toggle animates thumb movement instead of jumping.
    - The active/inactive visual change feels immediate and smooth.
    - Accessibility role, checked state, disabled behavior, and controlled value contract remain intact.
  </behavior>
  <action>Add an internal `Animated.Value` or equivalent built-in React Native animation state synchronized from the controlled `value` prop. Interpolate the thumb translation from off to on. For track color, prefer a layered active fill with animated opacity if direct color interpolation would require unsupported native-driver behavior. Keep `onValueChange(!value)` as the only state mutation path. Do not reintroduce React Native `Switch`, change `ThemeProvider`, or add a dependency.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint</automated>
    <automated>cd apps/mobile; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The thumb transition is animated when `value` changes.
    - The toggle remains readable in light and dark themes.
    - `accessibilityRole="switch"` and checked/disabled accessibility state remain correct.
  </acceptance_criteria>
  <done>UAT Test 2 has a concrete code fix path for the laggy-feeling toggle transition.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Make dictionary formSheet content-sized</name>
  <files>apps/mobile/src/presentation/app/shared/BubbleSheet/BubbleSheet.tsx, apps/mobile/src/presentation/app/shared/DictionaryWordDetailsSheet.tsx, apps/mobile/app/dictionary-word-details.tsx</files>
  <read_first>
    - `apps/mobile/src/presentation/app/shared/BubbleSheet/BubbleSheet.tsx`
    - `apps/mobile/src/presentation/app/shared/DictionaryWordDetailsSheet.tsx`
    - `apps/mobile/app/dictionary-word-details.tsx`
    - `apps/mobile/app/_layout.tsx`
    - `.planning/debug/phase-01-dictionary-sheet.md`
  </read_first>
  <behavior>
    - Dictionary details in the native `formSheet` route are measured by intrinsic content height.
    - Full-screen absolute overlay behavior remains available for BubbleSheet consumers that need a scrim.
    - Loading and missing-word states are visually distinct so the route does not flash "Word not found" while lookup is pending.
  </behavior>
  <action>Extend `BubbleSheet` with a typed content-sized/native-sheet mode, or a similarly explicit prop, that avoids the full-screen absolute root when `showScrim={false}` is used inside a native `formSheet`. Use that mode in `DictionaryWordDetailsSheet`. Keep existing overlay behavior for default BubbleSheet usage. Update `dictionary-word-details.tsx` to track loading separately from not-found, then pass a state that lets the sheet show a loading message before unresolved lookup is confirmed. Preserve the existing route configuration with `presentation: 'formSheet'` and `sheetAllowedDetents: 'fitToContents'` unless device testing proves it cannot work.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint</automated>
    <automated>cd apps/mobile; npm run typecheck</automated>
    <automated>cd apps/mobile; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Native formSheet consumers can render BubbleSheet content without a full-screen absolute root.
    - Dictionary loading, not-found, and resolved states are explicit and readable.
    - No native iOS/Android project files, package dependencies, Supabase, AI, sync, persistence, or domain logic are changed.
  </acceptance_criteria>
  <done>UAT Test 3 has a concrete code fix path for the unchanged broken dictionary window.</done>
</task>

</tasks>

<verification>
Run the canonical mobile checks after implementation:

1. `cd apps/mobile; npm run test -- src/presentation/theme/tokens.test.ts`
2. `cd apps/mobile; npm run lint`
3. `cd apps/mobile; npm run typecheck`
4. `cd apps/mobile; npm run build`

Then re-run `$gsd-verify-work 1` and repeat all three UAT checkpoints on Expo Go or an iOS simulator.
</verification>

<success_criteria>
- Phase 01 UAT Test 1 no longer reports press feedback as insufficiently tactile.
- Phase 01 UAT Test 2 no longer reports a laggy-feeling toggle state change.
- Phase 01 UAT Test 3 no longer reports the dictionary word sheet as the same broken window.
- Lint, typecheck, focused token test, and build pass after implementation.
- Changes remain scoped to the approved presentation-layer files and route loading state.
</success_criteria>
