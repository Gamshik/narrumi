---
phase: 02-shell-and-series-screens
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/mobile/src/presentation/app/screens/HomeScreen.tsx
  - apps/mobile/src/presentation/app/MobileApp.styles.ts
autonomous: true
requirements:
  - SCR-02
  - MOT-03
requirements_addressed:
  - SCR-02
  - MOT-03
must_haves:
  truths:
    - "SCR-02/D-01: Home leads with a create-first hero while saved series remain visible below."
    - "D-02: Saved series render as soft Bubble/Sorbet mini-cards with a clear continuation action."
    - "D-03: The primary create action appears in the hero only; the header carries identity/status."
    - "D-04: No saved-series state is folded into the hero rather than a separate empty card."
    - "MOT-03/D-16: Home loading, empty, deleting, disabled, error, and offline-relevant states use stable Bubble/Sorbet status UI."
    - "Floating tab bar clearance remains intact for final home content on small mobile viewports."
  artifacts:
    - path: "apps/mobile/src/presentation/app/screens/HomeScreen.tsx"
      provides: "Create-first Bubble/Sorbet home screen preserving local series actions."
    - path: "apps/mobile/src/presentation/app/MobileApp.styles.ts"
      provides: "Home hero, series mini-card, status, and tab-clearance style keys."
  key_links:
    - from: "HomeScreen.tsx"
      to: "localAppServices"
      via: "existing list/create/delete/generate setup calls"
      pattern: "presentation forwards user intent only"
    - from: "MobileApp.styles.ts"
      to: "src/presentation/theme/layout.ts"
      via: "existing screenContent bottom padding"
      pattern: "getFloatingTabBarContentPadding"
---

<objective>
Refresh the Home screen into the Bubble/Sorbet create-first shell while preserving existing series browsing and continuation behavior.

Purpose: Deliver SCR-02 and the home-specific locked decisions D-01 through D-04 without changing local-first application behavior.
Output: Updated `HomeScreen.tsx` layout and styles for hero, saved series mini-cards, home statuses, and tab-safe scrolling.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-shell-and-series-screens/02-CONTEXT.md
@.planning/phases/02-shell-and-series-screens/02-RESEARCH.md
@.planning/phases/02-shell-and-series-screens/02-PATTERNS.md
@concept/prd_concept_mvp.md
@stack/tech_stack_mvp.md
@architecture/architecture_for_ai.md
@design/design_system.html
@design/design_system_guidelines.md
@design/bubble/home.png
@apps/mobile/README.md
@apps/mobile/package.json
@.planning/phases/02-shell-and-series-screens/02-01-SUMMARY.md
@apps/mobile/src/presentation/app/screens/HomeScreen.tsx
@apps/mobile/src/presentation/app/MobileApp.styles.ts
@apps/mobile/src/presentation/theme/layout.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert Home header and hero to create-first Bubble layout</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Restyle the top of `HomeScreen` to match `design/bubble/home.png` while implementing D-01, D-03, and D-04. The header should show identity/status information and no duplicate create control. The hero should be the single primary create invitation and should open the existing create-series modal through the existing `setIsCreateOpen(true)` path. When `series.length === 0`, fold the empty-state explanation into the hero copy and do not render a separate empty card or sample series. Preserve `loadSeries`, focus reload, `errorMessage`, `isCreateOpen`, modal state, and all `localAppServices` calls.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
  </verify>
  <done>Home top hierarchy implements D-01/D-03/D-04, the hero opens the existing modal, and no application/persistence behavior moved into visual helpers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle saved series as Bubble mini-cards</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Replace dense saved-series row presentation with Bubble/Sorbet mini-card presentation per D-02 and SCR-02. Each saved series card must show title, level/genre/tone or equivalent compact metadata already available from the `Series` data, a clear continue/open action, and a compact delete action that preserves the existing delete confirmation and `deletingSeriesId` loading state. Use `BubbleSurface`, `BubbleButton`, `BubblePill`, `JellyPressable`, and `BubbleStatus` from shared exports where appropriate. Do not add fields to the domain model or create new navigation destinations.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
  </verify>
  <done>Saved series are readable Bubble/Sorbet mini-cards with continuation and deletion still wired to the existing callbacks for SCR-02/D-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve home state feedback and tab clearance</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts, apps/mobile/src/presentation/theme/layout.test.ts</files>
  <behavior>
    - MOT-03/D-16: Home load errors, create/save/generate/delete loading, disabled actions, and no-series state remain visible with text and not color alone.
    - SCR-02: Final saved-series card or final hero content can scroll above the floating tab bar.
    - VIS-04 dependency: Existing floating tab helper tests continue to pass.
  </behavior>
  <action>Wire home error/loading/deleting/disabled states through `BubbleStatus` or equivalent Bubble/Sorbet rows near the affected actions. Preserve `screenContent` bottom padding derived from the Phase 1 floating tab helper; do not reduce final scroll clearance. Keep the existing create modal mounted through the current component path so Plan 03 can refresh the setup form without reworking home routing. Run the existing layout tests to guard tab clearance behavior.</action>
  <verify>
    <automated>cd apps/mobile; npm run test -- src/presentation/theme/layout.test.ts && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Home state feedback satisfies MOT-03/D-16, final content remains clear of the floating tab bar, and layout tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local store/application use cases -> Home UI | Series records and operation states are untrusted display data crossing into presentation. |
| User actions -> localAppServices | Create/open/delete/generate actions must stay behind existing application services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | `HomeScreen` create/delete callbacks | medium | mitigate | Preserve existing callbacks and delete confirmation; change presentation only. |
| T-02-05 | Denial of Service | Floating tab clearance | low | mitigate | Keep helper-derived screen content padding and run `layout.test.ts`. |
| T-02-06 | Information Disclosure | Home error/status display | low | mitigate | Use existing safe messages and `BubbleStatus` rather than raw internal error dumps. |
| T-02-SC | Tampering | npm installs | low | accept | No package installs or dependency changes are planned. |
</threat_model>

<verification>
Run after this plan from `apps/mobile`:
- `npm run test -- src/presentation/theme/layout.test.ts`
- `npm run lint`
- `npm run typecheck`

Manual visual check: home header has no duplicate create action, hero is the only primary create entry, empty state lives in the hero, saved series cards can be continued/deleted, and the final card clears the floating tab bar.
</verification>

<success_criteria>
Plan 02-02 succeeds when Home aligns with `design/bubble/home.png`, preserves series browse/open/delete/create behavior, and satisfies D-01 through D-04 plus MOT-03 home state feedback.
</success_criteria>

<output>
Create `.planning/phases/02-shell-and-series-screens/02-02-SUMMARY.md` when done.
</output>
