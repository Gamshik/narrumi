---
phase: 02-shell-and-series-screens
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/mobile/src/presentation/app/screens/HomeScreen.tsx
  - apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx
  - apps/mobile/src/presentation/app/MobileApp.styles.ts
autonomous: true
requirements:
  - SCR-03
  - MOT-03
requirements_addressed:
  - SCR-03
  - MOT-03
must_haves:
  truths:
    - "SCR-03/D-05: Create and edit setup keep existing modal behavior while matching design/bubble/newseries.png."
    - "D-06: Setup field order follows the mockup where possible while preserving validation, role behavior, and existing AI setup generation rules."
    - "D-07: Character setup uses full editable Bubble/Sorbet cards with visible name and description fields."
    - "D-08: The setup form keeps one clear Generate action for the existing AI setup assist and does not add AI capabilities."
    - "MOT-03/D-16: Setup validation, disabled, generating, saving, read-only, and error states stay visible near affected actions."
  artifacts:
    - path: "apps/mobile/src/presentation/app/screens/HomeScreen.tsx"
      provides: "Bubble/Sorbet create-series modal presentation."
    - path: "apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx"
      provides: "Bubble/Sorbet edit-series setup modal presentation with read-only lock."
    - path: "apps/mobile/src/presentation/app/MobileApp.styles.ts"
      provides: "Shared setup modal, field, segmented control, character card, and status styles."
  key_links:
    - from: "HomeScreen.tsx"
      to: "localAppServices.generateSeriesSetupDraft.execute"
      via: "existing Generate action"
      pattern: "buildSetupDraftRequest remains unchanged unless style-only names require movement"
    - from: "SeriesDetailsScreen.tsx"
      to: "canEditSetup"
      via: "existing edit lock after first episode"
      pattern: "editable=false and disabled controls stay enforced"
---

<objective>
Refresh the create/edit series setup modal into the Bubble/Sorbet setup experience.

Purpose: Deliver SCR-03 and D-05 through D-08 while preserving setup validation, role rules, AI setup-assist boundaries, and post-first-episode read-only behavior.
Output: Updated setup modal presentation in create and edit flows with rounded fields, segmented controls, Generate action, character cards, and stable status feedback.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-shell-and-series-screens/02-CONTEXT.md
@.planning/phases/02-shell-and-series-screens/02-RESEARCH.md
@.planning/phases/02-shell-and-series-screens/02-PATTERNS.md
@concept/prd_concept_mvp.md
@stack/tech_stack_mvp.md
@architecture/architecture_for_ai.md
@design/design_system.html
@design/design_system_guidelines.md
@design/bubble/newseries.png
@apps/mobile/README.md
@apps/mobile/package.json
@.planning/phases/02-shell-and-series-screens/02-02-SUMMARY.md
@apps/mobile/src/presentation/app/screens/HomeScreen.tsx
@apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx
@apps/mobile/src/presentation/app/MobileApp.styles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle create-series setup modal</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <behavior>
    - SCR-03/D-05: Opening the existing home create action shows the same native modal behavior with Bubble/Sorbet layout.
    - D-06: Title, CEFR, genre, tone, participation, premise, role, and character fields remain controlled by existing form state and validation.
    - D-08: Generate calls the existing setup draft use case and does not alter list-selected fields beyond current behavior.
  </behavior>
  <action>Restyle `CreateSeriesModal`, `ChoiceGroup`, `FormField`, and related create setup sections in `HomeScreen.tsx` to match `design/bubble/newseries.png`. Preserve the native `Modal`, `KeyboardAvoidingView`, safe-area inset handling, `form`, `formErrors`, `validateSeriesForm`, `buildSetupDraftRequest`, `generateSetupDraft`, and `submitSeries` behavior. Use rounded Bubble fields, segmented level/genre/tone/participation controls, one prominent Generate action for the existing setup draft use case per D-08, and inline `BubbleStatus` feedback for validation/generating/saving. Do not create client-side AI behavior, change selected-list generation rules, or move validation into a visual-only primitive.</action>
  <verify>
    <automated>cd apps/mobile; npm run test -- src/application/useCases/generateSeriesSetupDraft.test.ts src/application/useCases/createSeries.test.ts && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Create setup modal matches the new-series target while preserving save, validation, AI setup draft, disabled, generating, and saving behavior for SCR-03/D-05/D-06/D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle edit setup modal and read-only states</name>
  <files>apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <behavior>
    - SCR-03: Existing not-yet-started series can still be edited through the setup modal.
    - D-11: Setup editing remains a small header action and becomes visibly read-only after the first episode.
    - MOT-03/D-16: Read-only, disabled, saving, generating, validation, and error states remain visible with accessible text.
  </behavior>
  <action>Restyle `SeriesSetupModal`, `SetupFormField`, `SetupChoiceGroup`, and related edit setup sections in `SeriesDetailsScreen.tsx` to match the same Bubble/Sorbet setup language as the create modal. Preserve `canEditSetup`, `editable={false}`, disabled controls, post-first-episode lock behavior, setup generation behavior, validation, save behavior, and route callbacks. Use inline status rows for read-only explanation, validation messages, generating, saving, and errors. Keep setup editing as a small header action per D-11; do not promote it into the strongest series detail action.</action>
  <verify>
    <automated>cd apps/mobile; npm run test -- src/application/useCases/updateSeriesSetup.test.ts src/application/useCases/generateSeriesSetupDraft.test.ts && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Edit setup modal is visually aligned with the new-series target, edit locking remains enforced, and state feedback satisfies MOT-03/D-16.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Upgrade character setup cards</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Apply D-07 in both create and edit setup flows by rendering character setup as full Bubble/Sorbet cards with visible name and description fields, add/remove controls, validation text, and disabled/read-only styling where applicable. Keep the existing `characterProfiles`, `mainCharacters`, `userRole`, and Character mode requirement behavior. Ensure add/remove actions remain unavailable while busy or read-only, and ensure card labels make the editable contract clear without relying on color alone. If code is extracted, extract only display-field/card groups that receive typed props and callbacks; extracted components must not import `localAppServices`, domain rules, persistence, sync, AI, or navigation.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
  </verify>
  <done>Character setup uses full editable Bubble/Sorbet cards in both create and edit flows, while Character mode requirements and read-only behavior remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User setup input -> validation/use cases | Title, premise, role, and character fields are untrusted and must remain validated before save/generation. |
| Generate action -> Edge Function path | AI setup draft generation must stay behind the existing application use case and backend boundary. |
| Existing series episode count -> editability | Episode history controls whether setup is editable and must not be bypassed by styling changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | Setup validation | high | mitigate | Preserve existing validation functions and run create/update setup use-case tests. |
| T-02-08 | Elevation of Privilege | Generate setup draft | high | mitigate | Keep Generate wired only to `localAppServices.generateSeriesSetupDraft.execute`; do not add direct AI/provider calls. |
| T-02-09 | Tampering | Post-first-episode read-only lock | medium | mitigate | Preserve `canEditSetup`, disabled controls, and non-editable inputs in the edit modal. |
| T-02-SC | Tampering | npm installs | low | accept | No package installs or dependency changes are planned. |
</threat_model>

<verification>
Run after this plan from `apps/mobile`:
- `npm run test -- src/application/useCases/generateSeriesSetupDraft.test.ts src/application/useCases/createSeries.test.ts src/application/useCases/updateSeriesSetup.test.ts`
- `npm run lint`
- `npm run typecheck`

Manual visual check: create setup modal and edit setup modal match `newseries.png` layout intent, Generate is one clear action, character cards are fully visible/editable where allowed, and read-only state is visually clear after first episode.
</verification>

<success_criteria>
Plan 02-03 succeeds when SCR-03 is true for both create and edit setup flows, D-05 through D-08 are implemented exactly, and setup state feedback remains accessible and stable.
</success_criteria>

<output>
Create `.planning/phases/02-shell-and-series-screens/02-03-SUMMARY.md` when done.
</output>
