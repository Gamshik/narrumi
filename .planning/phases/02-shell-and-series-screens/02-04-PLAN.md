---
phase: 02-shell-and-series-screens
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx
  - apps/mobile/src/presentation/app/MobileApp.styles.ts
autonomous: true
requirements:
  - SCR-04
  - MOT-03
requirements_addressed:
  - SCR-04
  - MOT-03
must_haves:
  truths:
    - "SCR-04/D-09: Series details put the continue/prep-next-episode card immediately below the header as the strongest visual priority."
    - "D-10: Series memory is hidden when empty and shown as a richer Bubble/Sorbet card only when memory content exists."
    - "D-11: Setup editing stays as a small header action with clear disabled/read-only styling after the first episode."
    - "D-12: Episode history uses soft episode cards with title, summary, status, and compact read/delete actions."
    - "MOT-03/D-16: Details loading, error, deleting, disabled, read-only, and empty-history states use stable Bubble/Sorbet feedback."
  artifacts:
    - path: "apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx"
      provides: "Bubble/Sorbet series detail hierarchy and episode history presentation."
    - path: "apps/mobile/src/presentation/app/MobileApp.styles.ts"
      provides: "Series detail header, priority card, memory card, episode card, and status styles."
  key_links:
    - from: "SeriesDetailsScreen.tsx"
      to: "localAppServices.loadSeriesDetails.execute"
      via: "existing focus reload"
      pattern: "presentation renders returned state only"
    - from: "SeriesDetailsScreen.tsx"
      to: "onContinueEpisode|onPrepareEpisode|onOpenEpisode|onReadSeries"
      via: "existing route callbacks"
      pattern: "no route ownership moves into cards"
---

<objective>
Refresh the Series Details screen hierarchy, memory display, and episode history into the Bubble/Sorbet series target.

Purpose: Deliver SCR-04 and D-09 through D-12 while preserving continue/prep/read/delete/setup-edit behavior.
Output: Updated `SeriesDetailsScreen.tsx` and styles for header, priority continue/prep card, conditional memory, episode cards, and status feedback.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-shell-and-series-screens/02-CONTEXT.md
@.planning/phases/02-shell-and-series-screens/02-RESEARCH.md
@.planning/phases/02-shell-and-series-screens/02-PATTERNS.md
@concept/prd_concept_mvp.md
@stack/tech_stack_mvp.md
@architecture/architecture_for_ai.md
@design/design_system.html
@design/design_system_guidelines.md
@design/bubble/series.png
@apps/mobile/README.md
@apps/mobile/package.json
@.planning/phases/02-shell-and-series-screens/02-03-SUMMARY.md
@apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx
@apps/mobile/src/presentation/app/MobileApp.styles.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote continue/prep card and header hierarchy</name>
  <files>apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Restyle the series details header and primary action hierarchy to match `design/bubble/series.png` and implement D-09 and D-11. The continue/prep-next-episode card must be the strongest visual priority immediately below the header, and it must preserve existing `latestEpisode`, `hasEpisodeInProgress`, `onContinueEpisode`, and `onPrepareEpisode` behavior. Keep setup editing as a small header action and use clear disabled/read-only styling after the first episode. Preserve loading and error states from `loadDetails`; render them with `BubbleStatus` or equivalent Bubble/Sorbet state UI.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
  </verify>
  <done>The details screen prioritizes continue/prep correctly for SCR-04/D-09, keeps setup editing small for D-11, and preserves existing route callbacks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render memory only when content exists</name>
  <files>apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Implement D-10 by hiding the series memory section when there is no meaningful memory content, and rendering a richer Bubble/Sorbet memory card only when `lastEpisodeSummary`, `unresolvedCliffhanger`, or another existing memory field contains content. Do not invent memory values or add an empty-memory card. Preserve the existing memory data contract and avoid changing application/domain memory behavior.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
  </verify>
  <done>Series memory is absent when empty and visible as a styled card only when existing memory content is present, satisfying D-10 without domain changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restyle episode history cards and deletion state</name>
  <files>apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Restyle episode history for D-12 and SCR-04 as soft Bubble/Sorbet cards with title, summary, completion/status metadata, and compact read/delete actions. Preserve `onOpenEpisode`, `requestDeleteEpisode`, `deletingEpisodeId`, delete confirmation, and loading/disabled behavior. Use visible text plus disabled/accessibility state for deleting and unavailable actions. Keep empty episode history feedback stable and styled without suggesting any new product flow.</action>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
  </verify>
  <done>Episode history cards expose title, summary, status, read, and delete behavior while maintaining deletion state and satisfying D-12/MOT-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local details load -> details UI | Series, memory, and episode data are untrusted local records crossing into presentation. |
| User route actions -> navigation callbacks | Continue, prepare, read, setup edit, and delete actions must stay routed through existing callbacks and services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | Continue/prep routing | medium | mitigate | Preserve existing latest-episode branching and route callbacks while changing only presentation hierarchy. |
| T-02-11 | Information Disclosure | Memory card | low | mitigate | Render only existing memory fields and do not expose raw local record internals. |
| T-02-12 | Tampering | Episode deletion | medium | mitigate | Preserve delete confirmation, deleting state, and disabled feedback on episode cards. |
| T-02-SC | Tampering | npm installs | low | accept | No package installs or dependency changes are planned. |
</threat_model>

<verification>
Run after this plan from `apps/mobile`:
- `npm run lint`
- `npm run typecheck`

Manual visual check: series details match `series.png` hierarchy, primary continue/prep card appears immediately below the header, memory hides when empty, episode cards expose read/delete state, and setup edit is small/read-only after first episode.
</verification>

<success_criteria>
Plan 02-04 succeeds when SCR-04 is true, D-09 through D-12 are implemented exactly, and series detail state feedback satisfies MOT-03 without changing application behavior.
</success_criteria>

<output>
Create `.planning/phases/02-shell-and-series-screens/02-04-SUMMARY.md` when done.
</output>
