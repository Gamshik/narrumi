---
phase: 02-shell-and-series-screens
plan: 06
type: execute
wave: 6
depends_on:
  - 02-05
files_modified:
  - apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx
  - apps/mobile/src/presentation/app/screens/HomeScreen.tsx
  - apps/mobile/src/presentation/app/MobileApp.styles.ts
autonomous: true
gap_closure: true
requirements:
  - SCR-01
  - SCR-02
  - MOT-03
requirements_addressed:
  - SCR-01
  - SCR-02
  - MOT-03
must_haves:
  truths:
    - "UAT-1/SCR-01: Typing in the auth email field does not cause the keyboard or centered auth panel to jump, recenter, or visibly rerender."
    - "UAT-2/SCR-02/D-01: The Home create story panel is shorter, create-first, and visually closer to design/bubble/home.png."
    - "UAT-3/SCR-02/D-02: Saved series render as compact Bubble/Sorbet mini-cards with readable metadata and preserved open/delete actions."
    - "MOT-03/D-16: Auth and Home loading, error, deleting, disabled, and empty states remain visible with text and accessible disabled state."
  artifacts:
    - path: "apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx"
      provides: "Stable keyboard-aware auth layout that preserves existing sign-in/sign-up behavior."
    - path: "apps/mobile/src/presentation/app/screens/HomeScreen.tsx"
      provides: "Compact home hero and saved-series mini-card presentation."
    - path: "apps/mobile/src/presentation/app/MobileApp.styles.ts"
      provides: "Auth keyboard stabilization and compact home card/hero style keys."
  key_links:
    - from: "AuthenticationScreen.tsx"
      to: "useAuthSession"
      via: "existing signIn/signUp callbacks"
      pattern: "No direct Supabase SDK calls are introduced in presentation."
    - from: "HomeScreen.tsx"
      to: "localAppServices"
      via: "existing list/create/delete/generate setup calls"
      pattern: "Presentation changes do not alter application service calls."
    - from: "MobileApp.styles.ts"
      to: "src/presentation/theme/layout.ts"
      via: "existing screenContent tab-safe padding"
      pattern: "Do not reduce floating tab clearance."
---

<objective>
Close the Phase 2 UAT gaps for auth keyboard stability and Home compactness.

Purpose: Fix the reported auth keyboard jumping and bring Home closer to `design/bubble/home.png` without changing authentication, local series, generation, deletion, or navigation behavior.
Output: Updated auth layout, compact Home create panel, compact saved-series mini-cards, and focused verification for SCR-01/SCR-02/MOT-03.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-shell-and-series-screens/02-CONTEXT.md
@.planning/phases/02-shell-and-series-screens/02-RESEARCH.md
@.planning/phases/02-shell-and-series-screens/02-PATTERNS.md
@.planning/phases/02-shell-and-series-screens/02-UAT.md
@.planning/phases/02-shell-and-series-screens/02-01-SUMMARY.md
@.planning/phases/02-shell-and-series-screens/02-02-SUMMARY.md
@concept/prd_concept_mvp.md
@stack/tech_stack_mvp.md
@architecture/architecture_for_ai.md
@design/design_system.html
@design/design_system_guidelines.md
@design/bubble/auth.png
@design/bubble/home.png
@apps/mobile/README.md
@apps/mobile/package.json
@apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx
@apps/mobile/src/presentation/app/screens/HomeScreen.tsx
@apps/mobile/src/presentation/app/MobileApp.styles.ts
@apps/mobile/src/presentation/theme/layout.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Stabilize authentication keyboard behavior</name>
  <files>apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <read_first>
    - `.planning/phases/02-shell-and-series-screens/02-UAT.md` gap 1 for the diagnosed keyboard jump root cause.
    - `apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx` current `KeyboardAvoidingView`, `TextInput`, `submit`, and `AuthModeButton` structure.
    - `apps/mobile/src/presentation/app/MobileApp.styles.ts` current `authKeyboardView`, `authContent`, `authCard`, and form input styles.
  </read_first>
  <action>Replace the full-screen centered keyboard avoidance that recenters the auth panel during controlled email typing with a stable layout. Keep the auth panel visually aligned with `design/bubble/auth.png`, but make keyboard movement local to the form content: use a stable outer container and either a scroll/content wrapper or a narrower keyboard-aware region so `justifyContent: "center"` is not recalculated for the whole screen on every email keystroke. Preserve `mode`, `email`, `password`, `isSubmitting`, `message`, `isError`, `submit`, `signIn`, `signUp`, secure password behavior, `onSubmitEditing`, and disabled submit logic. Do not add dependencies, direct Supabase calls, new auth flows, or new navigation.</action>
  <acceptance_criteria>
    - Typing continuously in the email field keeps keyboard position stable and does not visibly recenter the auth card.
    - Sign In and Create Account still use `useAuthSession` and keep the existing message handling.
    - The submit button remains disabled while inputs are empty or submission is in progress and exposes disabled semantics through existing BubbleButton behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
    <manual>Run Expo, open auth, focus email on a phone/simulator, type at least 20 characters, and confirm the keyboard and auth panel remain visually stable.</manual>
  </verify>
  <done>UAT gap 1 is closed: auth typing no longer causes keyboard jumping while SCR-01 behavior remains intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Shorten the Home create story panel</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <read_first>
    - `.planning/phases/02-shell-and-series-screens/02-UAT.md` gap 10 and test 2.
    - `.planning/phases/02-shell-and-series-screens/02-CONTEXT.md` decisions D-01, D-03, and D-04.
    - `apps/mobile/src/presentation/app/screens/HomeScreen.tsx` current `CreateHero`, `HomeHeader`, and create-modal wiring.
    - `design/bubble/home.png` target create panel proportions.
  </read_first>
  <action>Reduce the Home hero/create panel height and copy density while preserving D-01, D-03, and D-04. Keep the hero as the single primary create action, keep the header free of duplicate create controls, and keep the no-series message folded into the hero. Shorten visible copy, reduce vertical padding/gaps, avoid oversized title/body combinations inside the hero, and keep the existing `setIsCreateOpen(true)` create action path. Do not remove the hero create button, change modal behavior, or introduce a separate empty-state card.</action>
  <acceptance_criteria>
    - The Home create panel is visibly shorter than the current shipped version and closer to the compact proportions in `home.png`.
    - The primary create action remains in the hero only and still opens `CreateSeriesModal`.
    - Empty-state guidance appears inside the hero when no series exist, with no separate empty card or sample placeholder.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile; npm run lint && npm run typecheck</automated>
    <manual>Compare Home against `design/bubble/home.png` in light and dark themes; confirm the create panel is compact and the final content remains above the floating tab bar.</manual>
  </verify>
  <done>UAT gap 10 is closed: the Home create story panel no longer feels oversized while preserving SCR-02 and D-01/D-03/D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compress saved series mini-cards</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts, apps/mobile/src/presentation/theme/layout.test.ts</files>
  <read_first>
    - `.planning/phases/02-shell-and-series-screens/02-UAT.md` gaps 9 and 10 for oversized series card feedback.
    - `.planning/phases/02-shell-and-series-screens/02-CONTEXT.md` decision D-02.
    - `apps/mobile/src/presentation/app/screens/HomeScreen.tsx` current `SeriesList` and `SeriesCard` rendering.
    - `apps/mobile/src/presentation/theme/layout.test.ts` existing floating tab clearance guard.
  </read_first>
  <behavior>
    - UAT-3/D-02: Each saved series card is a compact mini-card, not a large story panel.
    - SCR-02: Opening and deleting a series still call the existing callbacks.
    - VIS-04 dependency: Final saved-series card remains clear of the floating tab bar.
  </behavior>
  <action>Restyle `SeriesCard` and related styles into compact mini-cards. Reduce card padding, inter-card gaps, body copy, and action height. Limit premise/body text more aggressively or remove it if the card can still show title, compact metadata, CEFR, and a clear continuation/open action. Keep delete available as a compact secondary/destructive action and preserve `deletingSeriesId`, delete confirmation, `onOpenSeries`, and `onDeleteSeries`. Keep cards soft Bubble/Sorbet mini-cards per D-02, not dense flat list rows. Do not add domain fields, change sorting, or change the create/list/delete application services.</action>
  <acceptance_criteria>
    - Saved series cards are visibly smaller and scan like compact mini-cards from `home.png`.
    - Each card keeps a clear open/continue target and a compact delete target.
    - Deleting state remains visible and disabled for the affected card.
    - `layout.test.ts` still confirms tab-bar clearance math.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile; npm run test -- src/presentation/theme/layout.test.ts && npm run lint && npm run typecheck</automated>
    <manual>Populate at least two saved series and compare the card size/spacing with `design/bubble/home.png`; verify last card scrolls above the floating tab bar.</manual>
  </verify>
  <done>UAT gap 9 is closed: saved series cards are compact Bubble/Sorbet mini-cards and Home behavior remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User credentials -> AuthProvider | Email/password input remains untrusted and must stay behind `useAuthSession`. |
| User actions -> localAppServices | Home create/open/delete actions must stay behind existing application services. |
| Local records -> Home UI | Saved series data is untrusted display data and should be rendered safely without changing domain shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Elevation of Privilege | `AuthenticationScreen` | high | mitigate | Preserve `useAuthSession` boundary; do not import Supabase SDK or mutate sessions directly in presentation. |
| T-02-06-02 | Tampering | `HomeScreen` delete/open callbacks | medium | mitigate | Preserve existing delete confirmation, `deletingSeriesId`, and route callbacks while changing only presentation. |
| T-02-06-03 | Denial of Service | Home scroll/tab clearance | low | mitigate | Keep helper-derived bottom padding and run `layout.test.ts`. |
| T-02-06-SC | Tampering | npm installs | low | accept | No package installs or dependency changes are planned. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Bring non-reader shell and series-management flows into Bubble/Sorbet visual alignment | Covered by plan 02-06 for auth and Home gap closure. |
| REQ | SCR-01 auth screen aligned with `auth.png` | Covered by Task 1. |
| REQ | SCR-02 Home aligned with `home.png` | Covered by Tasks 2 and 3. |
| REQ | MOT-03 stable visible states | Covered by Tasks 1 and 3. |
| RESEARCH | Reuse Phase 1 primitives; no new dependencies | Covered by all tasks and threat model. |
| RESEARCH | Preserve Clean Architecture boundaries | Covered by all tasks and threat model. |
| CONTEXT | D-01 create-first hero | Covered by Task 2. |
| CONTEXT | D-02 saved series mini-cards | Covered by Task 3. |
| CONTEXT | D-03 create action in hero only | Covered by Task 2. |
| CONTEXT | D-04 empty state folded into hero | Covered by Task 2. |
| UAT | Auth keyboard jumping | Covered by Task 1. |
| UAT | Home create story panel too large | Covered by Task 2. |
| UAT | Saved series cards too large | Covered by Task 3. |
</source_audit>

<verification>
Run from `apps/mobile` after implementation:
- `npm run test -- src/presentation/theme/layout.test.ts`
- `npm run lint`
- `npm run typecheck`

Manual visual UAT: auth keyboard typing stability, compact Home hero, compact saved-series cards, light/dark readability, and floating tab clearance against `design/bubble/auth.png` and `design/bubble/home.png`.
</verification>

<success_criteria>
Plan 02-06 succeeds when UAT gaps 1, 2, and 3 are closed without changing auth/session behavior, local-first series behavior, or Phase 1 tab clearance.
</success_criteria>

## Artifacts this phase produces

- Modified `apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx` stable auth keyboard layout.
- Modified `apps/mobile/src/presentation/app/screens/HomeScreen.tsx` compact create hero and series mini-cards.
- Modified `apps/mobile/src/presentation/app/MobileApp.styles.ts` auth/home compact style keys.

<output>
Create `.planning/phases/02-shell-and-series-screens/02-06-SUMMARY.md` when done.
</output>
