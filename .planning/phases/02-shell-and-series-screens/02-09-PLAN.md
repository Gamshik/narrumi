---
phase: 02-shell-and-series-screens
plan: 09
type: execute
gap_closure: true
wave: 2
depends_on: []
files_modified:
  - apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx
  - apps/mobile/src/presentation/app/MobileApp.styles.ts
  - apps/mobile/src/presentation/app/screens/HomeScreen.tsx
  - apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx
  - apps/mobile/src/presentation/app/screens/SettingsScreen.tsx
  - apps/mobile/src/presentation/app/shared/BubbleSegmentedControl/BubbleSegmentedControl.tsx
  - apps/mobile/src/presentation/app/shared/BubbleSegmentedControl/index.ts
  - apps/mobile/src/presentation/app/shared/index.ts
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04
  - SCR-09
requirements_addressed:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04
  - SCR-09
must_haves:
  truths:
    - "The authentication screen KeyboardAvoidingView properly wraps ScrollView and avoids center-justification that causes field overlap."
    - "Home screen hero and series cards use appropriate, minimal spacing."
    - "Create series and edit setup modals have left-aligned header titles with inline top-right actions, removing the bottom border and inner generate row."
    - "Series details continue/prep card is a static surface with a specific button, not a giant pressable card."
    - "Settings uses a custom BubbleSegmentedControl instead of the native iOS component."
  artifacts:
    - path: "apps/mobile/src/presentation/app/shared/BubbleSegmentedControl/BubbleSegmentedControl.tsx"
      provides: "Bubble/Sorbet styled segmented control primitive."
  key_links: []
---

<objective>
Execute gap closure for Phase 02 based on 02-UAT.md. Resolve styling issues across Auth, Home, Series Details, and Settings screens to fully align with Bubble/Sorbet guidelines.
</objective>

<execution_context>
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/workflows/execute-plan.md
@D:/ProgrammingAndProjects/learn-english/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-and-series-screens/02-UAT.md
@apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx
@apps/mobile/src/presentation/app/MobileApp.styles.ts
@apps/mobile/src/presentation/app/screens/HomeScreen.tsx
@apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx
@apps/mobile/src/presentation/app/screens/SettingsScreen.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix Auth Screen Layout</name>
  <files>apps/mobile/src/presentation/app/auth/AuthenticationScreen/AuthenticationScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Fix issue where keyboard covers fields and flickers. In `AuthenticationScreen.tsx`, invert the hierarchy so `KeyboardAvoidingView` wraps the `ScrollView`, instead of `ScrollView` wrapping `KeyboardAvoidingView`. In `MobileApp.styles.ts`, remove `justifyContent: 'center'` from `authScrollContent` and use margins/padding to vertically center content instead, allowing proper scroll behavior.</action>
  <verify>
    <automated>cd apps/mobile && npm run lint && npm run typecheck</automated>
  </verify>
  <done>KeyboardAvoidingView correctly wraps ScrollView and center justification is replaced with padding.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix Home Screen Hero and Series Cards</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Fix oversized UI elements on the Home screen. In `HomeScreen.tsx`, change `CreateHero` to use `variant='card'` (or reduce internal spacing) to ensure a minimal, design-accurate layout. In `MobileApp.styles.ts`, separate layout rules from visual sizing rules. For the series cards in `HomeScreen.tsx`, pass sizing overrides to `contentStyle` instead of the `style` prop of `BubbleButton`, and add a `paddingVertical` override in `MobileApp.styles.ts` for compact buttons.</action>
  <verify>
    <automated>cd apps/mobile && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Home screen hero and saved series mini-cards are compact and match minimal Bubble design.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix Setup Modals and Series Details Card Layout</name>
  <files>apps/mobile/src/presentation/app/screens/HomeScreen.tsx, apps/mobile/src/presentation/app/screens/SeriesDetailsScreen.tsx, apps/mobile/src/presentation/app/MobileApp.styles.ts</files>
  <action>Fix setup modals and series details banner UI. In `HomeScreen.tsx` and `SeriesDetailsScreen.tsx`, restyle the `modalHeader` for a large, left-aligned title without a bottom border. Move "Generate" and "Save" buttons into the right side of this new header, removing `setupGenerateRow` from the form body. In `SeriesDetailsScreen.tsx`, change the `continueBanner` to a static `View` or `BubbleSurface`, and move the `JellyPressable` to only wrap the text, applying `styles.bannerButton`.</action>
  <verify>
    <automated>cd apps/mobile && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Modal headers are left-aligned without bottom borders, containing top-right actions, and the continue/prep banner uses a static card with an inner button.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Implement BubbleSegmentedControl for Settings</name>
  <files>apps/mobile/src/presentation/app/shared/BubbleSegmentedControl/BubbleSegmentedControl.tsx, apps/mobile/src/presentation/app/shared/BubbleSegmentedControl/index.ts, apps/mobile/src/presentation/app/shared/index.ts, apps/mobile/src/presentation/app/screens/SettingsScreen.tsx</files>
  <action>Replace native iOS SegmentedControl with a custom Bubble-styled primitive. Create `BubbleSegmentedControl` in `shared/` using `BubbleSurface`/`BubblePill` logic to switch between values (e.g., CEFR levels). Export it from the shared barrel. Update `SettingsScreen.tsx` to use this new primitive for the CEFR selection instead of the native control.</action>
  <verify>
    <automated>cd apps/mobile && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Custom BubbleSegmentedControl replaces the native iOS SegmentedControl in SettingsScreen.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-UAT-1| Tampering | npm installs | low | accept | No package installs or dependency changes are planned. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| UAT | Gap 1 | Covered by Task 1 |
| UAT | Gap 2 | Covered by Task 2 |
| UAT | Gap 3 | Covered by Task 2 |
| UAT | Gap 4 | Covered by Task 3 |
| UAT | Gap 5 | Covered by Task 3 |
| UAT | Gap 6 | Covered by Task 4 |
</source_audit>

<verification>
Run after this plan from `apps/mobile`:
- `npm run lint`
- `npm run typecheck`
</verification>

<success_criteria>
All UAT gaps from 02-UAT.md are resolved, and the code passes linting and typechecking.
</success_criteria>

<output>
Create `.planning/phases/02-shell-and-series-screens/02-09-SUMMARY.md` when done.
</output>
